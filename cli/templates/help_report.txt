usage: report [output] [classpath(s)]

    Coverts a mcover data file into a standalone report

Arguments:

   out        : path to output directory or file
   classpath  : path to classpath src matching coverage input, repeatable

Optional params
	
	 -[target] : target (cpp|neko|php|js|as3)

Example:

    mcover report bin/coverage src
